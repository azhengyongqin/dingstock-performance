# AGENTS.md

本仓库同时使用 Claude Code,Codex,Cursor进行开发。

## 语言

默认使用中文回答。涉及代码时，为关键业务逻辑、配置边界和非显而易见的实现补充简洁中文注释。

## 必读规则

开始任何代码修改前，必须先读取并遵守根目录 `CLAUDE.md`。

`CLAUDE.md` 是本项目的主规则文档，包含沟通约定、Agent skills 配置、项目定位、仓库结构、后端开发约定、前端开发约定，以及数据库、Prisma、飞书、Redis、shadcn、Data Table 等强制规则。

## 优先级

如本文件与 `CLAUDE.md` 冲突，以 `CLAUDE.md` 为准。

## 教程说明

教程类内容请写成小白能照着做的步骤，并标注成功标志。
